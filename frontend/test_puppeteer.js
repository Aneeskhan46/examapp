const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.setContent('<!DOCTYPE html><html><head><script defer src='https://unpkg.com/mathlive'></script></head><body><math-field id='mf'>\\\\text{A}</math-field></body></html>'); await page.waitForTimeout(2000); await page.evaluate(() => { const mf = document.getElementById('mf'); mf.selection = {ranges: [[0, 1]]}; mf.executeCommand(['applyStyle', {fontSeries: 'b'}]); }); const val = await page.evaluate(() => document.getElementById('mf').value); console.log('VALUE:', val); await browser.close(); })();
